{% extends "layouts/default.twig" %}

{% block content %}
    <section class="section">
        <div class="container">
            <div class="columns is-multiline">
                {% for post in posts %}
                    <div class="column is-half">
                        {% include 'components/card-post.twig' %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>
{% endblock %}