{% extends "index.twig" %}

{% block content %}
    <section class="section">
        <div class="container">
            <h1>{{ title }}</h1>
            <p>{{ description }}</p>
        </div>
    </section>
    {{ parent() }}
{% endblock %}
