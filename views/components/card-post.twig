<div class="card">
    <div class="card-content">
        <p class="title is-4">{{ post.title }}</p>

        <p>
            {{ post.preview.length(30).read_more(false) }}
        </p>
        <p>
            <a href="{{ post.link }}" class="">
                Lire plus
            </a>
        </p>
    </div>
</div>